import express, { Request, Response, NextFunction } from "express";

// var express = require('express');
const ranger = express.Router();

ranger.get('/', (req: Request, res: Response, next: NextFunction) => {

})


export default ranger;